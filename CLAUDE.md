# CLAUDE.md

此文件为 Claude Code (claude.ai/code) 在本代码库中工作提供指导。

## 项目概述

这是一个针对 **A股和 ETF 的海龟交易系统**实现。系统采用经典的海龟交易策略，并针对中国市场进行了适配，包括趋势跟踪、突破检测、仓位管理和风险控制。

## 架构设计

代码库采用**模块化架构**，职责分离清晰：

### 核心模块

1. **turtle_config.py** - 集中配置文件
   - 所有策略参数（资金、风险系数、均线周期、放量阈值）
   - ETF 代码前缀，用于自动识别资产类型
   - 被监控脚本和评估脚本共同使用

2. **turtle_core.py** - 核心交易逻辑（无界面）
   - 通过 AkShare 获取数据（自动识别 ETF 和股票）
   - 技术指标：TR、ATR(N)、唐奇安通道、MA60、成交量均线
   - 市场状态分析（趋势、成交量、波动率）
   - 信号检测（突破/跌破，带过滤条件）
   - 仓位计算（基于账户资金和 N 值）
   - 止损计算（固定止损、移动止损、盈利保护）
   - 加仓点位计算（每上涨 0.5N 加仓一次）

3. **turtle_tech.py** - 持仓监控脚本
   - 分析 `HOLDINGS` 列表中的持仓（第 28-39 行）
   - 输出盈亏、信号、止损位、加仓点位
   - 提供每日汇总，包括排名和操作建议

4. **turtle_scout.py** - 新股评估脚本
   - 交互式命令行，用于评估潜在建仓机会
   - 入场点分析（55 日和 20 日突破）
   - 建仓前风险评估

### 数据流向

```
turtle_config.py (参数配置)
        ↓
turtle_core.py (获取数据 → 计算指标 → 分析状态 → 检测信号)
        ↓
    ┌───────────────────────┐
    ↓                       ↓
turtle_tech.py        turtle_scout.py
(监控持仓)            (评估新股)
```

## 运行系统

### 监控现有持仓
```bash
python turtle_tech.py
```
运行前需在 `HOLDINGS` (turtle_tech.py:28-39) 中更新持仓列表。

### 评估新股机会
```bash
python turtle_scout.py
```
交互模式 - 输入 6 位股票/ETF 代码，输入 'q' 退出。

### 调整策略参数
编辑 `turtle_config.py`：
- `TOTAL_CAPITAL` - 账户总资金
- `RISK_RATIO` - 每笔交易风险系数（默认 1%）
- `TREND_MA_PERIOD` - 趋势过滤均线周期（默认 60）
- `VOLUME_RATIO_THRESHOLD` - 放量阈值（默认 1.2 倍）

## 关键技术细节

### 数据来源
- 使用 **AkShare** (`akshare` 库) 获取免费中国市场数据
- 通过代码前缀自动识别 ETF 和股票（参见配置中的 `ETF_PREFIXES`）
- 所有数据均为前复权（`adjust="qfq"`）

### 信号逻辑 (turtle_core.py:149-200)
- **买入信号**：55 日高点（系统 1）或 20 日高点（系统 2）
- **卖出信号**：10 日低点（短线离场）或 20 日低点（完全清仓）
- **过滤条件**：趋势向下（价格 < MA60）、成交量不足、波动率收缩时拒绝信号
- 唐奇安通道使用 `shift(1)` 避免未来函数

### 仓位计算 (turtle_core.py:203-218)
- 单元头寸 = (资金 × 风险系数) / N
- 按 100 股整数倍取整（A股市场要求）
- 加仓点位：基准价 +0.5N、+1.0N、+1.5N（最多加仓 3 次）

### 止损机制 (turtle_core.py:221-264)
- **新建仓位**：入场价 - 2N
- **加仓后**：最后加仓价 - 2N
- **盈利 > 10%**：启用移动止损（20 日最高价 - 2N），最低锁定成本价
- 输出中会标注止损类型

### 持仓配置
`HOLDINGS` 在 turtle_tech.py 中是一个字典列表，包含：
- `code`：6 位股票/ETF 代码（必填）
- `name`：显示名称（必填）
- `cost`：加权平均成本价（必填，0 表示仅观察）
- `shares`：总持仓股数（必填）
- `last_add_price`：最近一次加仓价格（可选，用于止损计算）

## 依赖安装

安装所需包：
```bash
pip install akshare pandas
```

## 重要提示

- **数据质量**：AkShare 免费但可能有延迟或缺失，重要交易请以券商数据为准
- **唐奇安通道**：使用 `shift(1)` 避免未来函数，通道值反映前一日的区间
- **N 值**：20 日 ATR，所有风险计算的核心（仓位、止损、加仓）
- **A股特性**：100 股整数倍交易、ETF 前缀识别、前复权处理
